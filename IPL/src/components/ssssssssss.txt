.auction-container {
  display: flex;
  justify-content: space-between;
  padding: 20px;
  font-family: 'Roboto', sans-serif;
  background: linear-gradient(135deg, #2b5876, #4e4376); /* Same theme */
  color: #ffffff;
  width: 97vw;
  height: 86vh;
  box-sizing: border-box; /* Ensures padding doesn't affect width and height */
  border-radius: 10px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

/* Owners section */
.owner-card {
  border: 1px solid rgba(255, 255, 255, 0.2); /* Light border for owner card */
  padding: 15px;
  margin-bottom: 14px;
  background-color: rgba(255, 255, 255, 0.1);
  border-radius: 8px;
  margin-left: 15px;
  min-width: max-content;
}